# `library`

Standalone libraries that are reusable outside the compiler package tree.

## Packages

- `cstc_ansi_color`: ANSI color policy + escape emission helpers
- `cstc_error_report`: header-only Rust-like diagnostic renderer built on byte offsets
